name: My First Workflow
on: [push] 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Muti line commands
        run: |
          echo "This is a multi-line command"
          ls -la
          cat README.md
          